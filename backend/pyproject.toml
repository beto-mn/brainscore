[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
# B008: FastAPI's dependency-injection idiom (`= File(...)`, `= Depends(...)`)
# always calls a function in an argument default; that's the intended usage.
ignore = ["B008"]
