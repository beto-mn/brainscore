# Public HTTPS proxy URL of the RunPod pod (port 8000).
# Changes every time the pod is recreated — never hardcode it in the code.
# Example: https://abcd1234-8000.proxy.runpod.net
NEXT_PUBLIC_API_URL=
